# 文章題解決｜固定実装ルール（2026-09-01改訂）

対象：割合・比・速さ。

## 共通の最上位原則
- 「大きな記入フォーム」ではなく、児童が順に考えるための足場として設計する。
- A4縦、1ページ1問。
- 未知量・途中計算結果・最終答えを問題用紙側へ先出ししない。
- 線の物理的な長さに数学的意味がある場合、数量関係と一致させる。
- SVG の stroke / stroke-width は要素に直接指定し、印刷時の線消失を防ぐ。

## 割合｜考える7段階
1. 読む：`／で区切る → 声に出して読む → 求めるものに○`
2. 拾う：問題文から必要な数・量を抽出する
3. 分ける：量の役割へ分類する
4. 基準を決める：100％にあたる量を判断する
5. 線分図を完成させる
6. 関係 → 式：言葉の関係を数式へ変換する
7. たしかめ → 答え：100％との大小関係などで結果を見積もる

## 問題難易度と支援段階を分離する
- 問題難易度：基礎／標準／発展。
- 支援段階：A導入／B標準／C自立。
- A/B/Cは問題の難しさではなく、同じ認知課題に対してどこまで足場を与えるかを表す。
- A：必要な数・量の候補、100％の選択、基準位置などを多く提示する。
- B：記述中心。100％など最小限の基準だけを提示する。
- C：抽出・分類・基準判断・位置決めを児童が自分で行う。
- C自立では、コード内部で計算した正答位置を線分図へ先に描かない。2本の基準線と0位置程度から自分で構成させる。

同じ認知を `選択 → 記述 → 本文判断` へ移し、形式暗記を避ける。

## 固定問題と類題自動生成を分離する
- `word-solution-rate`：精査済み割合30問をそのまま保持する固定問題DBモード。
- `word-solution-rate-auto`：固定版とは別の類題自動生成モード。
- 固定30問は `word-problem-rate-fixed-db-v1.js` に保存する。
- 固定問題の問題文・分類・計算・答えをランダム生成器で置き換えない。
- 類題自動生成では問題型の配分を先に決め、同じ型だけに偏らないようにする。

## たしかめ
- `大きい・小さい`、`多い・少ない` などの判断は児童が選ぶ。
- コード側が正しい側だけを問題用紙へ印刷しない。
- 計算前の見積もりと計算後の答えを比較させる。

## 割合の表現
- 問題文が百分率なら、作図でも百分率を維持し、不要な分数化をしない。
- 必要な問題だけ【カザマ式 補強】を表示する。
  - `もとにする量 × 割合 ＝ 比べる量`
  - `「もとにする量 の 割合」では、「の」→「×」として式につなげる`
- この `の→×` は割合表現に限定し、日本語一般の「の」へ一般化しない。

## 比・速さの線分図ルール
- 比の1目盛りは常に同じ幅とする。
- 比の連結・構造変化では共通する量を同じ物理長にそろえる。
- 速さは原則として上段=道のり、下段=時間を同じ位置で対応させる。

## 実装ID
- `word-solution-rate`：文章題解決｜割合［固定30問］
- `word-solution-rate-auto`：文章題解決｜割合［類題自動生成］
- `word-solution-ratio`：文章題解決｜比
- `word-solution-speed`：文章題解決｜速さ

関連ファイル：
- `word-problem-rate-fixed-db-v1.js`
- `word-problem-thinking-rate-v2.js`
- `word-problem-thinking-rate-v2.css`
- `word-problem-solution-v1.js`
- `word-problem-solution-v1.css`
